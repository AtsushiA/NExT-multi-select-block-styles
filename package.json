{
  "name": "next-multi-select-block-styles",
  "version": "1.0.1",
  "description": "ブロックエディタのスタイル選択を複数選択可能にします。",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "test:e2e": "npx playwright test",
    "test:e2e:ui": "npx playwright test --ui"
  },
  "lint-staged": {
    "*.php": "composer run phpcs --"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@wordpress/e2e-test-utils-playwright": "^1.47.0",
    "@wordpress/env": "^11.7.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0"
  }
}
